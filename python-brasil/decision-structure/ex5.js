// Faça um Programa que leia três números e mostre o maior e o menor deles.

const numbers = [455, 5, 100]
let biggest = numbers[0]

for (number of numbers) {
  if (number > biggest) biggest = number
}

console.log(biggest)
